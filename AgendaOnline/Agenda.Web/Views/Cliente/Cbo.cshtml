@model IEnumerable<AgendaOnline.Dominio.Cbo>

@{
    ViewBag.Title = "Cbo";
}

<h2>Cbo</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.IDCBO }) |
                @Html.ActionLink("Details", "Details", new { id = item.IDCBO }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.IDCBO})
            </td>
        </tr>
    }

</table>
